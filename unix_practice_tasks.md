# Практические задания по Unix командам

## Подготовка

### Создание рабочей среды

1. Откройте терминал
2. Перейдите в директорию с файлом `unix_practice_setup.sh`
3. Выполните команды:
```bash
chmod +x unix_practice_setup.sh
./unix_practice_setup.sh
cd unix_practice
```

**Время выполнения всех заданий: 30-40 минут**

---

## Часть 1: Навигация (pwd, cd, ls) - 10 минут

### Задание 1.1 - Где я нахожусь?

**Команда:** `pwd` (print working directory)

```bash
# Узнайте ваше текущее местоположение
pwd
```

**Вопрос:** Запишите полный путь к вашей текущей директории.

---

### Задание 1.2 - Что здесь есть?

**Команда:** `ls` (list)

```bash
# Посмотрите содержимое текущей директории
ls

# Посмотрите с дополнительной информацией
ls -l

# Посмотрите все файлы (включая скрытые)
ls -la
```

**Вопрос:** Сколько директорий вы видите в корне учебной среды?

---

### Задание 1.3 - Перемещение между директориями

**Команда:** `cd` (change directory)

```bash
# Перейдите в домашнюю директорию студента
cd home/student

# Проверьте где вы
pwd

# Вернитесь назад на один уровень
cd ..

# Проверьте снова
pwd

# Перейдите обратно
cd home/student
```

**Задача:** Перейдите в директорию `Documents/Work` и выведите её содержимое.

<details>
<summary>💡 Подсказка</summary>

```bash
cd Documents/Work
ls
```
</details>

---

### Задание 1.4 - Абсолютные и относительные пути

```bash
# Вернитесь в корень учебной среды
cd ../..

# Теперь перейдите в Pictures/Vacation используя полный путь от текущей позиции
cd home/student/Pictures/Vacation

# Посмотрите содержимое
ls

# Вернитесь в корень
cd ../../../..
```

**Задача:** Из корня перейдите в `Projects/Python/scripts` одной командой.

---

## Часть 2: Просмотр содержимого (cat, head, tail, less) - 5 минут

### Задание 2.1 - Чтение файлов

**Команда:** `cat` (concatenate)

```bash
# Перейдите в Documents/Work
cd home/student/Documents/Work

# Прочитайте содержимое отчёта
cat report_jan.txt

# Прочитайте несколько файлов сразу
cat report_jan.txt report_feb.txt
```

---

### Задание 2.2 - Просмотр начала и конца файлов

**Команды:** `head`, `tail`

```bash
# Перейдите в директорию с логами
cd ../../../var/log

# Посмотрите первые 5 строк файла
head -n 5 system.log

# Посмотрите последние 5 строк
tail -n 5 system.log
```

**Задача:** Прочитайте содержимое файла `error.log` и запишите, что там написано.

---

## Часть 3: Создание и удаление (mkdir, touch, rm, rmdir) - 8 минут

### Задание 3.1 - Создание директорий

**Команда:** `mkdir` (make directory)

```bash
# Перейдите в вашу домашнюю директорию
cd ../../home/student

# Создайте новую директорию
mkdir MyFirstFolder

# Проверьте что она создалась
ls

# Создайте вложенные директории сразу
mkdir -p MyProjects/WebDev/HTML

# Проверьте
ls -R MyProjects
```

**Задача:** Создайте структуру директорий: `Practice/Week1/Day1`

---

### Задание 3.2 - Создание файлов

**Команда:** `touch`

```bash
# Создайте пустой файл
touch test.txt

# Создайте несколько файлов сразу
touch file1.txt file2.txt file3.txt

# Проверьте
ls -l
```

**Задача:** Создайте 5 файлов с названиями: task1.txt, task2.txt, task3.txt, task4.txt, task5.txt

---

### Задание 3.3 - Удаление файлов и директорий

**Команды:** `rm` (remove), `rmdir`

```bash
# Удалите один файл
rm test.txt

# Удалите несколько файлов
rm file1.txt file2.txt

# Проверьте
ls

# Попробуйте удалить пустую директорию
mkdir EmptyFolder
rmdir EmptyFolder

# Удалите директорию с содержимым (ОСТОРОЖНО!)
rm -r MyFirstFolder
```

**Задача:** 
1. Удалите файлы task1.txt и task2.txt
2. Удалите директорию `Practice` со всем содержимым

⚠️ **Важно:** Команда `rm -r` удаляет безвозвратно! Будьте осторожны!

---

## Часть 4: Копирование и перемещение (cp, mv) - 8 минут

### Задание 4.1 - Копирование файлов

**Команда:** `cp` (copy)

```bash
# Перейдите в Documents/Personal
cd Documents/Personal

# Скопируйте файл
cp notes.txt notes_backup.txt

# Проверьте
ls

# Скопируйте файл в другую директорию
cp shopping_list.txt ../../Desktop/

# Проверьте что файл скопировался
ls ../../Desktop/
```

**Задача:** Скопируйте все файлы из `Documents/School` в директорию `Backup`

<details>
<summary>💡 Подсказка</summary>

```bash
cd ../..
cp Documents/School/* Backup/
ls Backup/
```
</details>

---

### Задание 4.2 - Копирование директорий

```bash
# Скопируйте директорию целиком
cp -r Documents/Work Documents/Work_Backup

# Проверьте
ls Documents/
```

**Задача:** Создайте копию директории `Projects/Website` с именем `Projects/Website_Old`

---

### Задание 4.3 - Перемещение и переименование

**Команда:** `mv` (move)

```bash
# Переименуйте файл
cd Desktop
mv todo.txt my_todo.txt

# Проверьте
ls

# Переместите файл в другую директорию
mv quick_notes.txt ../Documents/Personal/

# Проверьте что файла больше нет в Desktop
ls
# Проверьте что файл появился в Personal
ls ../Documents/Personal/
```

**Задача:** 
1. Переименуйте файл `important_file.txt` в `very_important.txt`
2. Переместите файл `archive_me.txt` в директорию `Backup`

---

## Часть 5: Поиск (find, grep) - 7 минут

### Задание 5.1 - Поиск файлов по имени

**Команда:** `find`

```bash
# Вернитесь в домашнюю директорию
cd ~/unix_practice/home/student

# Найдите все .txt файлы
find . -name "*.txt"

# Найдите файлы с определённым именем
find . -name "notes.txt"

# Найдите все директории с именем "Work"
find . -type d -name "Work"
```

**Задача:** Найдите все файлы с расширением `.mp3` в вашей домашней директории

---

### Задание 5.2 - Поиск текста в файлах

**Команда:** `grep`

```bash
# Найдите строку в файле
grep "Отчёт" Documents/Work/report_jan.txt

# Найдите строку во всех .txt файлах
grep "список" Documents/Personal/*.txt

# Найдите строку без учёта регистра
grep -i "отчёт" Documents/Work/*.txt

# Найдите строку во всех файлах рекурсивно
grep -r "фото" Pictures/
```

**Задача:** Найдите в каких файлах в директории `Documents` встречается слово "Конспект"

---

## Часть 6: Комбинированные задания - 7 минут

### Задание 6.1 - Организация файлов

Выполните следующие действия по порядку:

1. Создайте директорию `Organized` в вашей домашней директории
2. Внутри создайте три поддиректории: `TextFiles`, `Images`, `Code`
3. Скопируйте все .txt файлы из `TestFiles` в `Organized/TextFiles`
4. Скопируйте все .jpg и .png файлы из `Pictures/Vacation` в `Organized/Images`
5. Скопируйте все .py файлы из `Projects/Python/scripts` в `Organized/Code`

<details>
<summary>💡 Решение</summary>

```bash
# 1. Создание структуры
mkdir Organized
mkdir Organized/TextFiles Organized/Images Organized/Code

# 2-4. Копирование файлов
cp TestFiles/*.txt Organized/TextFiles/
cp Pictures/Vacation/*.jpg Organized/Images/
cp Pictures/Vacation/*.png Organized/Images/
cp Projects/Python/scripts/*.py Organized/Code/

# 5. Проверка
ls Organized/TextFiles/
ls Organized/Images/
ls Organized/Code/
```
</details>

---

### Задание 6.2 - Очистка

Выполните следующее:

1. Найдите все файлы со словом "old" в имени
2. Переместите их в директорию `Backup`
3. Удалите все пустые файлы task*.txt если они остались

<details>
<summary>💡 Решение</summary>

```bash
# 1. Поиск файлов с "old"
find . -name "*old*"

# 2. Перемещение
mv old_file_2020.txt Backup/
mv old_file_2021.txt Backup/

# 3. Удаление task файлов
rm task*.txt
```
</details>

---

### Задание 6.3 - Создание отчёта

Создайте файл с отчётом о выполненной работе:

1. Создайте файл `report.txt` в домашней директории
2. Добавьте в него список всех директорий в `Projects`
3. Добавьте количество файлов в `Documents`

<details>
<summary>💡 Решение</summary>

```bash
# 1. Создание файла
touch report.txt

# 2. Список директорий (используя перенаправление вывода)
ls -l Projects/ > report.txt

# 3. Подсчёт файлов
find Documents/ -type f | wc -l >> report.txt

# Просмотр результата
cat report.txt
```
</details>

---

## Бонусные команды (если осталось время)

### Полезные команды для новичков

```bash
# Подсчёт строк, слов, символов в файле
wc filename.txt

# Сортировка содержимого файла
sort filename.txt

# Показать дерево директорий (если установлена команда tree)
tree

# Показать размер директории
du -sh Documents/

# Показать использование диска
df -h

# Очистить экран терминала
clear

# История команд
history

# Поиск команды в истории
history | grep "mkdir"
```

---

## Проверочный чек-лист

Убедитесь, что вы можете выполнить следующее без подсказок:

- [ ] Узнать текущую директорию (`pwd`)
- [ ] Показать содержимое директории (`ls`, `ls -la`)
- [ ] Перейти в другую директорию (`cd`)
- [ ] Создать директорию (`mkdir`)
- [ ] Создать файл (`touch`)
- [ ] Скопировать файл (`cp`)
- [ ] Переместить/переименовать файл (`mv`)
- [ ] Удалить файл (`rm`)
- [ ] Удалить директорию (`rm -r`)
- [ ] Прочитать файл (`cat`)
- [ ] Найти файл (`find`)
- [ ] Найти текст в файле (`grep`)

---

## Советы для продолжения обучения

1. **Всегда проверяйте результат** - после каждой команды используйте `ls` или `pwd` чтобы убедиться в результате
2. **Используйте Tab для автодополнения** - начните печатать имя файла и нажмите Tab
3. **Используйте стрелки вверх/вниз** - для просмотра истории команд
4. **Читайте справку** - команда `man ls` покажет руководство по команде `ls`
5. **Будьте осторожны с `rm`** - удалённые файлы не восстанавливаются!

---

## Что дальше?

После выполнения этих заданий вы можете изучить:

- Права доступа к файлам (`chmod`, `chown`)
- Работу с процессами (`ps`, `top`, `kill`)
- Перенаправление ввода/вывода (`>`, `>>`, `|`)
- Текстовые редакторы (`nano`, `vim`)
- Работу с архивами (`tar`, `zip`)
- Сетевые команды (`ping`, `curl`, `wget`)

---

## Очистка

Когда закончите практику, можете удалить учебную среду:

```bash
cd ..
rm -rf unix_practice
```

**Поздравляем с завершением практики! 🎉**

Теперь вы знаете основные команды Unix и готовы к дальнейшему изучению!

